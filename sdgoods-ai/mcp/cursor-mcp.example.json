{
  "mcpServers": {
    "sdgoods": {
      "command": "sdgoods-mcp",
      "args": [],
      "env": {
        "SDGOODS_API_BASE": "https://sdgoods.ai/api"
      },
      "_note": "Cursor 项目级 MCP 配置（存为仓库根 .cursor/mcp.json）。实际 MCP server 由谷仓 SDGOODS 开放平台服务端提供（不开源）。本配置不含任何 token；登录走邮箱验证码/OAuth 交互，由 server 侧处理。不要在此写入密钥。"
    }
  }
}
